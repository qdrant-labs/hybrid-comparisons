collection_name: dbpedia_float16_noquant # float16 base dtype, no quantization (baseline)
data:
  # do not change data path -- vectors are computed server-side via Qdrant Cloud Inference
  corpus:
    path: ../../data/dbpedia/corpus.jsonl
dense_vectors:
  dtype: float16
  memory: cached
colbert_vectors:
  dtype: float16
  memory: cached
  multivector_config:
    comparator: max_sim
  hnsw_config:
    m: 0 # indexing should be disabled since we are using these for rescoring (otherwise very expensive)
sparse_vectors:
  modifier: idf
