[project]
name = "qdrant-load"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { email = "astraberte9@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "pyyaml>=6.0.3",
    "qdrant-client>=1.19.0",
]

[project.scripts]
qdrant-load = "qdrant_load:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
